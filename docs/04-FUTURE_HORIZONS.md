# Future Horizons (Prioritized)

> **Parent Index:** [SKILL.md](./SKILL.md) — Read the root index first.

---

### Priority 1: Reference Library Vertical (Static File Rendering)
- **Goal:** Render markdown files dynamically from the local file system (bypassing Supabase) in the `repo-learning-coach` dashboard.
- **Collections to Support:** 
  1. User Manuals (Phase 1)
  2. Product Docs (`docs/`)
  3. Agent Skills (`.agents/skills/`)
  4. Obsidian Vault (`obsedian-vault-my-ob1`)

### Deferred / Icebox

- **Self-Hosted Kubernetes Deployment:** Adapt the OB1 helm charts/blueprints to create a fully local, self-contained PostgreSQL + pgvector deployment for `my-ob1` as a future-proofing measure.
